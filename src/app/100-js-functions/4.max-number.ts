/**
 * Write a function named maxNumber that receives n numbers as parameters and
 * returns the biggest one between them.
 */
export const maxNumber = (...numbers: number[]) => Math.max(...numbers);
